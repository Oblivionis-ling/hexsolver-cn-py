# 求解算法说明

这份文档总结当前项目的求解计算逻辑，重点对应 [solver.py](D:\Desktop\HexInfinite\hexsolver_cn_py\src\hexsolver_cn\solver.py) 中已经实现的约束建模与推理流程。

## 1. 输入与目标

求解器的输入是一个 `Board`：

- 棋盘上每个格子的轴坐标 `coord`
- 每个格子的状态：
  - `hidden`：未知格
  - `blue`：已知蓝格
  - `black`：已知黑格
  - `grey/outside`：不可用
- 已识别或人工确认的线索：
  - 黑格邻域线索
  - 蓝格范围线索
  - 外侧行线索
  - 顶部 `REMAINING`

求解器的输出不是“直接把整盘解完”，而是：

- 当前一定可以开的格子
- 当前一定必须标黑的格子
- 每一步为什么成立

也就是说，输出的是“**当前必然步**”。

## 2. 棋盘变量表示

在约束层里，只有未知格 `hidden` 会被建成布尔变量：

- `1` 表示这个未知格最终是蓝格
- `0` 表示这个未知格最终是黑格

已知蓝格和已知黑格不会再建变量，而是直接作为常量参与约束：

- 已知蓝格视为常量 `1`
- 已知黑格 / 灰格 / 不存在位置视为常量 `0`

## 3. 三类线索的约束对象

### 3.1 黑格线索

黑格线索只约束它的 **6 个直接邻居**。

- 普通数字 `n`：邻居蓝格总数等于 `n`
- `{n}`：邻居蓝格总数等于 `n`，并且这些蓝格在 6 邻域环上必须连续
- `-n-`：邻居蓝格总数等于 `n`，并且这些蓝格不能形成一个连续块

黑格是环形邻域，所以 `{n}` / `-n-` 都按 **循环序列** 处理。

### 3.2 蓝格线索

蓝格线索约束它 **半径 2 内的全部可玩格**，不包含它自己。

- 只支持普通数字 `n`
- 约束含义：范围内蓝格总数等于 `n`

### 3.3 外侧行线索

每条外侧行线索约束一整条线上所有可玩格。

- 普通数字 `n`
- `{n}`
- `-n-`

行线索不是环形，所以 `{n}` / `-n-` 按 **线性序列** 处理。

## 4. 约束规格化

求解器会先把所有线索统一转换成 `ConstraintSpec`：

- `label`：用于输出理由
- `clue_type`：`count / consecutive / nonconsecutive`
- `number`
- `coords`：这条线索实际约束到的格子坐标序列
- `cyclic`：是否按循环序列处理

这样后面不再区分“它来自黑格、蓝格还是外侧行”，而是按统一的约束模型推理。

## 5. 第一层：局部确定性推理

求解器优先做不用全局搜索的局部推理。

### 5.1 计数补满规则

对任意一条线索，统计：

- `known_blue`：当前已经确定为蓝的格子数
- `hidden`：当前仍未知的格子集合
- `need = number - known_blue`

然后有两种最基本的必然结论：

- 如果 `need == 0`
  - 说明需要的蓝格已经凑够
  - 那么这条线索里剩下的所有未知格都必须判黑

- 如果 `need == len(hidden)`
  - 说明剩下所有未知格都得拿来补蓝
  - 那么这些未知格都必须判蓝

### 5.2 连续 / 非连续排列推理

对 `{n}` 或 `-n-`，会枚举这条线索允许的所有局部排列。

如果某个未知格在 **所有合法排列** 里都只能是蓝，或都只能是黑，那么它就是必然步。

这里分两类：

- `{n}`：
  - 直接枚举所有连续块位置
- `-n-`：
  - 枚举所有蓝黑组合
  - 过滤掉那些“蓝格刚好形成单一连续块”的情况

### 5.3 子集差分规则

如果两条线索的未知格集合满足：

- `A_hidden ⊆ B_hidden`

那么可以比较它们各自还需要的蓝格数：

- 若 `A_need == B_need`
  - `B - A` 这部分差集不可能再提供蓝格
  - 差集全黑

- 若 `B_need - A_need == len(B - A)`
  - 差集里的所有格都必须拿来补蓝
  - 差集全蓝

这条规则对很多重叠线索非常有效。

### 5.4 REMAINING 全局补满规则

如果顶部 `REMAINING` 已知：

- `remaining_blue == 0`
  - 所有未知格全黑

- `remaining_blue == hidden_count`
  - 所有未知格全蓝

## 6. 第二层：全局约束求解

如果局部规则没有推出任何必然步，就进入全局模型。

当前项目使用的是 **OR-Tools CP-SAT**。

## 7. CP-SAT 模型怎么建

### 7.1 基本计数约束

对每条 `ConstraintSpec`，都会生成：

```text
sum(sequence) == number
```

其中 `sequence` 是这条线索对应的 0/1 序列。

### 7.2 `{n}` 连续约束

如果是连续线索 `{n}`，会额外引入“连续块起点”布尔变量 `start_i`。

约束逻辑是：

1. 只能选择一个起点
2. 被这个起点覆盖到的那一段格子必须为蓝
3. 其他格子必须为黑

线性行线索与黑格环形线索的区别，只体现在“覆盖索引”是否循环。

### 7.3 `-n-` 非连续约束

如果是非连续线索 `-n-`，在已经有 `sum(sequence) == number` 的前提下，再禁止出现长度刚好为 `n` 的完整连续块。

也就是对每个可能的长度为 `n` 的窗口，加：

```text
sum(run) <= n - 1
```

含义是：

- 你可以有 `n` 个蓝格
- 但它们不能正好全部连成一整块

### 7.4 REMAINING 约束

如果顶部剩余蓝格数已知，再加：

```text
sum(all_hidden_variables) == remaining_blue
```

## 8. 第三层：唯一性 forced move 检查

全局模型可行之后，求解器不会直接返回某个完整解，而是逐个检查未知格是否“被唯一确定”。

对每个未知格 `x`：

1. 假设 `x = 蓝`
2. 假设 `x = 黑`
3. 分别检查模型是否仍可行

结论：

- 如果“蓝可行、黑不可行”
  - 这个格必须判蓝
- 如果“黑可行、蓝不可行”
  - 这个格必须判黑
- 如果“两边都可行”
  - 当前还不是必然步

这一步给出的就是“全局唯一性推理”。

## 9. 为什么求解器是两段式的

当前实现不是一上来就把所有未知格丢进 SAT 全搜，而是：

1. 先做局部规则
2. 局部无果时再做全局 forced move

这样做的好处是：

- 理由更自然，更像人类解题过程
- 一般局面下更快
- 输出解释更可读

## 10. 当前实现的边界

### 10.1 优点

- 已覆盖 Infinite 常用核心规则
- 支持黑格、蓝格、行线索、REMAINING
- 支持 `{n}` 和 `-n-`
- 支持局部解释与全局唯一性解释

### 10.2 当前限制

- 还没有实现更强的高阶逻辑链推理
- 没有做“整盘最优解释排序”
- 结果是“当前必然步集合”，不是完整人类式逐步证明树
- OCR 错误会直接影响约束正确性

## 11. 如果后续继续升级

下一步最值得加强的方向通常有三类：

1. 更强的 OCR 与线索置信管理
2. 更强的局部推理规则库
3. 对 forced move 给出更细粒度的反证解释

## 12. 对应代码位置

- 约束规格化：`_constraint_specs`
- 局部规则：
  - `_apply_simple_count_rule`
  - `_apply_pattern_rule`
  - `_apply_subset_rule`
  - `_apply_remaining_rule`
- 全局模型：
  - `_solve_model`
  - `_add_constraint`
- 唯一性 forced move：
  - `_collect_global_forced_moves`
  - `_solve_with_assumption`

如果你后面要继续优化求解器，这个文件可以当成当前版本的算法基线。
