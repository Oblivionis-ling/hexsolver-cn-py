# 种子逐步求解器开发清单与版本规划

本文档定义从当前截图求解工作台演进到“输入种子、复刻地图、手动同步局面、询问下一步”的实施路线。

## 1. 产品目标

用户输入 `种子号 + 难度` 后，程序生成与指定 Steam 版本完全一致的初始地图。用户在可视化棋盘上把已经打开或标黑的格子同步到当前游戏状态，然后请求一个必然成立的下一步，并查看中文理由。

当前逆向基线：

- Steam Build ID：`5455383`
- Unity：`5.6.3f1`
- 游戏程序集 SHA-256：`835DEC694D7685809EDAC963E0F47306AD4300C7D7C9C555AD457F20EFAA8083`
- Hard 生成器：`LevelGenerator`，验证器：`Solver / Set`
- Easy 生成器：`OldLevelGenerator`，验证器：`MarvinHexcellsSolver`

## 2. 不可妥协的验收原则

- 只有同一个种子在原游戏与本工具中逐格、逐线索一致，才标记为“生成器已验证”。
- 近似生成、相似难度或“能解但地图不同”不算完成。
- 完整答案必须与公开盘面隔离。逐步求解器只能使用玩家当前可见信息。
- 每个建议都必须是必然步；无法证明时返回“当前没有可证明步骤”，不猜。
- Easy 与 Hard 分别实现和验收，不能用一套近似算法冒充两套生成器。

## 3. 模块划分

```text
seed + difficulty + game build
              |
              v
     SeedGeneratorBackend
              |
              v
 GeneratedPuzzle(public board + private answer)
              |
              v
      InteractiveSession
              |
              v
 HexReasoningSolver.next_step()
              |
              v
    coord + action + Chinese reason
```

## 4. 开发清单

### A. 基础架构

- [x] 逆向两代生成器和求解器入口。
- [x] 确认 `.hexcells` 编码、运行时对象树和坐标转换。
- [x] 建立种子请求、难度枚举、后端注册和一致性状态接口。
- [x] 建立公开盘面与私有答案分离的数据结构。
- [x] 建立手动盘面状态会话和单步求解接口。
- [x] 将种子请求、一致性门禁、局面会话和单步求解接入桌面 UI。
- [x] 建立隔离副本原版运行时桥接，支持 Easy/Hard 最终地图导出。
- [x] 后台生成、超时、失败恢复和原始程序集哈希门禁。
- [x] 默认后端移除 `Hexcells Infinite.exe` 调用，原版桥降为显式差分工具。

### B. Hard 生成器复刻

- [x] 固化 Unity 5.6.3 `Random` 的离线一致实现。
- [x] 复刻五种地图形状及坐标边界。
- [x] 完整复刻蓝黑分布、花格和三方向行线索。
- [x] 移植 `Solver / Set` 的提示添加验证循环。
- [x] 移植冗余提示与修饰符贪心删除。
- [x] 建立至少 30 个 Hard 种子的原游戏逐格对照样本。
- [x] seed 1–30 的所有对照样本达到 100% 一致。

### C. Easy 生成器复刻

- [x] 通过原版运行时桥接支持精确 Easy 种子地图。
- [x] 复刻 `seed + numberOfCalls` 的随机调用语义。
- [x] 用无 Unity 托管宿主执行旧形状、提示和初始揭示流程。
- [x] 用无 Unity 托管宿主直接执行 Marvin 的规则顺序和 OR 二选一传播。
- [ ] 对原实现已确认的方向错误做“兼容模式”和“修正版模式”区分。
- [ ] 建立至少 30 个 Easy 种子的逐格对照样本。
- [ ] 所有对照样本达到 100% 一致。

### D. 可视化与交互

- [x] 输入种子与选择 Easy/Hard。
- [x] 绘制无截图依赖、可缩放平移的六边形棋盘和行线索。
- [x] 选中格后可设为未知、已开蓝、已标黑。
- [x] 手动或按建议揭开格子时，同步释放原版覆盖层下的格内提示。
- [x] 支持撤销和恢复初始盘面。
- [ ] 支持重做。
- [x] 一次只高亮一个推荐步骤并展示中文理由。
- [ ] 在棋盘上同步高亮参与推理的完整线索集合。
- [ ] 保存/载入当前进度，不写入或修改游戏存档。

### E. 正确性与发布

- [x] Easy/Hard seed 1 原版导出和运行时生成回归测试。
- [x] 规则最小样例和 Qt 主流程测试。
- [x] 每一步与私有答案交叉验证，但答案不参与推理。
- [x] Easy 多种子与 Hard 五种形状代表种子的完整求解回放测试。
- [x] Windows 启动脚本和静态/真实生成诊断命令。
- [x] 增加默认后端不启动 `Hexcells Infinite.exe` 的回归测试。
- [x] 增加 Unity Y 轴到 Qt 场景的方向回归，防止地图上下镜像。
- [ ] Windows 打包冒烟测试。

## 5. 版本规划

### `0.2.0`：种子工作台基础

- 种子/难度输入和生成器接口。
- 独立棋盘渲染。
- 手动同步格子状态。
- “告诉我下一步”单步求解与中文理由。
- 未通过一致性验证的生成器明确拒绝使用。

### `0.3.0–0.3.1`：原版运行时桥接与完整回放

- Easy/Hard 均通过隔离副本调用原版生成器和内置求解器。
- UI 后台生成最终盘面，支持手动同步和连续询问下一步。
- 原始程序集哈希门禁、诊断命令和 Easy/Hard 冒烟测试。
- 覆盖层下提示按玩家进度释放，完整回放逐步核对原版答案。

### `0.4.0`：默认生成无游戏进程

- Hard `seed -> map` 纯 Python 精确复刻。
- Easy 通过无 Unity 托管宿主直接执行原版生成器与 Marvin。
- 默认产品链路不启动 `Hexcells Infinite.exe`；原版桥只做显式差分。

### `0.4.1–0.4.3`：显示方向与可核查解释

- 修正 Unity 到 Qt 的 Y 轴镜像与左右斜向标签族。
- 所有单步建议展开条件、坐标、数量和算式。
- 全局 forced move 给出错误假设、充分冲突条件和相反假设可行性。
- 长解释使用可滚动说明区承载。

### `0.5.0`：可用性与诊断

- 撤销/重做、局面存取、步骤历史和冲突定位。
- `.hexcells` 与 JSON 导入导出。
- 生成器版本/哈希诊断和种子对照报告。

### `1.0.0`：稳定发布

- Easy/Hard 完整支持。
- 回归种子、求解回放、打包运行全部通过。
- 普通用户无需接触游戏文件或命令行即可使用。

## 6. 当前开发状态

当前已完成 `0.4.3`：Hard 为纯 Python 精确移植，Easy 为无 Unity 托管核心；两者默认都不启动游戏。原版隔离运行时只保留为显式差分校验工具。Easy/Hard seed 1–50 及 `00000000`、`99999999` 两个边界种子已全部逐字段一致；Unity 到 Qt 的 Y 轴方向与镜像后的左右斜向标签均已修正，并用官方 seed 1/2 截图核对。单步求解理由已升级为带具体状态、算式、排列统计和全局反证冲突条件的可核查解释。
