# HexInfinite 种子求解器开发历史

本文记录项目从截图/OCR 原型到离线精确种子求解器的演进过程。它关注已经落地的能力、关键技术判断、验证证据和仍然存在的边界；未来版本完成后应继续追加，而不是改写旧记录。

## 时间线

| 日期 | 阶段 | 主要结果 |
|---|---|---|
| 2026-04-26 | 规则与路线调研 | 整理 Hexcells Infinite 的邻域、行线索、蓝格覆盖和 `REMAINING` 规则，并调研 SixCells、HexSolver、整数约束和 OCR 路线。原始研究资料现归档在 [`docs/research`](docs/research/README.md)。 |
| 2026-04-27 | `0.1.0` 截图求解原型 | 提交 `2f9dbcb`。建立 PySide6 中文界面、截图导入、六边形检测、模板/RapidOCR、盘面模型、确定性规则和 CP-SAT 全局兜底。此时输入仍以截图和人工校对为主。 |
| 2026-04-27–28 | 工程化与 OCR 评估 | 建立独立 Conda 环境、OCR 数据集规范、标注工具、诊断文档和早期 GitHub 同步脚本。后续确认 OCR 适合作为次级导入入口，不适合作为种子求解的主链路。 |
| 2026-08-02 | `0.2.x` 游戏风格工作台 | 根据选定的方案 2 重构为左侧操作栏、右侧原生六边形棋盘；加入手动三态、撤销、历史、单步提示、后台生成和错误恢复。盘面不再依赖截图贴图。 |
| 2026-08-02–03 | `0.3.0–0.3.1` 原版运行时桥 | 只读分析 Steam Build `5455383` 的 `Assembly-CSharp.dll`，定位 Hard 的 `LevelGenerator + Solver/Set` 和 Easy 的 `OldLevelGenerator + MarvinHexcellsSolver`。建立隔离副本、窄 TSV 导出合约、版本哈希门禁和完整逐步回放验证。 |
| 2026-08-03 | `0.4.0` 默认生成无游戏进程 | Hard 完成 Unity 5.6.3f1 RNG 与生成器的纯 Python 精确移植；Easy 通过最小 Unity API 兼容层直接执行原版托管类。默认产品链路不再启动 `Hexcells Infinite.exe`。 |
| 2026-08-04 | `0.4.1` 坐标方向修复 | 定位 Unity 世界 Y 轴向上、Qt 场景 Y 轴向下造成的整盘上下镜像，修正坐标投影并增加方向回归测试。 |
| 2026-08-04 | `0.4.2` 斜向标签修复 | 同步交换镜像后的左右斜向标签族，保留原版约束射线不变；Hard seed 1 的 `-4-`、`-5-`、`5` 标签斜率与官方画面重新一致。 |
| 2026-08-04 | 一键启动与首次 GitHub 发布 | 新增 `启动求解器.cmd`、环境/依赖自修复、托管核心构建和启动前诊断。提交 `96813b2`、`0527a26` 已推送到 `agent/offline-generator-and-launcher`，Draft PR #1 用于合并到远端 `main`。 |
| 2026-08-05 | 开发空间整理 | 将规则与路线资料纳入仓库；加入可公开的 Easy/Hard seed 1 TSV 夹具和最终 UI 证据；标准单元测试不再依赖工作区外的 `reverse_harness/exports`。重复截图、中间构建和失效同步脚本转入本地日期归档。 |
| 2026-08-05 | `0.4.3` 详细可核查推理解释 | 将局部计数、排列、子集差分、全盘剩余和 CP-SAT 全局步骤统一改成分段推导；全局反证提取足以无解的关键约束，界面改用可滚动说明区，并新增对应分支与长文本回归测试。 |

## 版本演进

### `0.1.0`：截图/OCR 中文求解助手

- 从完整游戏截图检测橙、蓝、黑、灰六边形。
- OCR 识别格内数字、外侧行线索和剩余数，并允许人工补录。
- 支持局部计数、连续/非连续排列、子集差分和 CP-SAT forced-move 检查。
- 证明“抽象盘面 + 精确约束 + 中文解释”的路线可行，但 OCR 准确率限制了实用性。

### `0.2.x`：种子解题台与原生棋盘

- 将主工作流改为种子、难度、当前玩家进度和单步建议。
- 将公开盘面与私有生成答案隔离；私有答案只参与测试核对，不参与求解。
- 使用 Qt 原生几何绘制棋盘，支持缩放、平移、目标高亮和动态提示释放。

### `0.3.0–0.3.1`：原版生成器桥接

- 通过隔离运行时确认生成器入口、种子格式、三方向行线索和公开/隐藏层。
- Easy、Hard 都能由同一个八位十进制种子重建。
- 建立 `schema -> parse -> board -> solve -> private-answer replay` 的验证闭环。

### `0.4.0`：离线精确生成

- Hard：纯 Python 重放形状、颜色、数字、花格、行线索、可解性校验和冗余线索裁剪。
- Easy：无 Unity 进程的托管宿主执行 `OldLevelGenerator + MarvinHexcellsSolver`。
- 原版隔离副本降为显式差分工具，默认运行不再启动游戏。

### `0.4.1–0.4.2`：官方显示方向一致

- 修正棋盘 Y 轴投影。
- 修正左右斜向标签镜像。
- 坐标、约束射线和显示标签分别建模，避免视觉修复改变生成内容。

### `0.4.3`：详细可核查推理解释

- 普通局部规则显示线索要求、已知蓝格、未知格、还需数量、坐标集合和计算式。
- 连续/非连续规则显示合法排列数量、排列样例及目标格在所有排列中的固定值统计。
- 子集规则展开两个条件的需求计算、包含关系、差集和需求差。
- 全局规则对错误颜色做反证，列出由 CP-SAT assumptions 返回的一组充分冲突条件，并再次验证相反颜色仍有可行解。
- 说明区改为可滚动只读文本，保留完整多行理由且不挤掉“计算/应用”按钮。

## 关键技术判断

1. **种子生成优先于 OCR。** 已知种子时直接重建结构化盘面，可靠性和解释能力都优于截图识别；OCR 继续保留为旧盘面或外部截图的次级入口。
2. **生成器和求解器分离。** 生成器产生公开盘面、私有答案和揭示元数据；求解器只读取当前公开状态。
3. **先局部、后全局。** 优先输出可读的局部计数、排列和子集差分；只有局部规则停住时才调用 CP-SAT 假设反证。
4. **内容差异和投影差异分开验证。** 先比较 TSV/对象，再比较屏幕坐标，避免把镜像问题误判为 RNG 或生成算法错误。
5. **原游戏保持只读。** 安装目录和存档不由本项目修改；需要启动原版做差分时，只使用本地隔离副本。

## 逆向确认的生成链路

- Hard：`LevelGenerator` 选择五类形状之一，随后执行格子与数字生成、简单解裁剪、行线索生成、`TryToSolve` 和冗余线索裁剪。
- Easy：`OldLevelGenerator` 使用独立的参数和随机调用顺序，并由 `MarvinHexcellsSolver` 验证。
- 两套生成器都接受八位十进制种子，但不是同一套算法。
- Hard 的原版内部求解器是生成阶段的 depth-2 验证器；应用中的 CP-SAT 全局反证是另一个独立求解后端。

## 验证记录

- Easy/Hard seed 1–50 与原版最终 TSV 逐字段、逐对象、逐顺序一致。
- `00000000`、`99999999` 两个边界种子的 Easy/Hard 输出一致。
- Hard 样本覆盖 Columnar、Diamond、Triangle、Hex、Hourglass 五种形状。
- 代表种子能够完整逐步回放到零未知格，每一步与私有答案一致。
- 当前自动测试为 41 项；有原版程序集时会额外执行 Easy 托管核心的真实集成校验，否则该项明确跳过。
- 启动器自检会验证托管核心、原版程序集哈希和默认后端不包含游戏进程。

## 当前边界与下一步

- Easy 仍需要用户合法安装版本中的 `Assembly-CSharp.dll`，仓库不会分发该文件。
- Hard 复杂种子忠实重放原版多轮求解与裁剪，少数种子可能需要几十秒。
- CP-SAT 全局步骤已提取足以造成无解的关键冲突条件，但它不是完整人工证明树，也不保证是数学上的最小或唯一冲突核心。
- 后续优先项见 [`ROADMAP.md`](ROADMAP.md)：性能、局面存取、冲突定位、导入导出和稳定发布。

## 文档与证据入口

- 项目说明：[`README.md`](README.md)
- 工作区边界：[`docs/WORKSPACE.md`](docs/WORKSPACE.md)
- 求解算法：[`docs/solver/ALGORITHM.md`](docs/solver/ALGORITHM.md)
- 种子开发清单：[`docs/generator/IMPLEMENTATION_PLAN.md`](docs/generator/IMPLEMENTATION_PLAN.md)
- 设计验收：[`docs/design/QA.md`](docs/design/QA.md)
- 历史研究资料：[`docs/research`](docs/research/README.md)
