# HexInfinite 种子求解器开发历史

本文记录项目从截图/OCR 原型到离线精确种子求解器的演进过程。它关注已经落地的能力、关键技术判断、验证证据和仍然存在的边界；未来版本完成后应继续追加，而不是改写旧记录。

## 时间线

| 日期 | 阶段 | 主要结果 |
|---|---|---|
| 2026-04-26 | 规则与路线调研 | 整理 Hexcells Infinite 的邻域、行线索、蓝格覆盖和 `REMAINING` 规则，并调研 SixCells、HexSolver、整数约束和 OCR 路线。原始研究资料现归档在 [`docs/research`](docs/research/README.md)。 |
| 2026-04-27 | `0.1.0` 截图求解原型 | 提交 `2f9dbcb`。建立 PySide6 中文界面、截图导入、六边形检测、模板/RapidOCR、盘面模型、确定性规则和 CP-SAT 全局兜底。此时输入仍以截图和人工校对为主。 |
| 2026-04-27–28 | 工程化与 OCR 评估 | 建立独立 Conda 环境、OCR 数据集规范、标注工具、诊断文档和早期 GitHub 同步脚本。后续确认 OCR 适合作为次级导入入口，不适合作为种子求解的主链路。 |
| 2026-08-02 | `0.2.x` 游戏风格工作台 | 根据选定的方案 2 重构为左侧操作栏、右侧原生六边形棋盘；加入手动三态、撤销、历史、单步提示、后台生成和错误恢复。盘面不再依赖截图贴图。 |
| 2026-08-02–03 | `0.3.0–0.3.1` 原版运行时桥 | 只读分析 Steam Build `5455383` 的 `Assembly-CSharp.dll`，定位 Hard 的 `LevelGenerator + Solver/Set` 和 Easy 的 `OldLevelGenerator + MarvinHexcellsSolver`。建立隔离副本、窄 TSV 导出合约、版本哈希门禁和完整逐步回放验证。 |
| 2026-08-03 | `0.4.0` 默认生成无游戏进程 | Hard 完成 Unity 5.6.3f1 RNG 与生成器的纯 Python 精确移植；Easy 通过最小 Unity API 兼容层直接执行原版托管类。默认产品链路不再启动 `Hexcells Infinite.exe`。 |
| 2026-08-04 | `0.4.1` 坐标方向修复 | 定位 Unity 世界 Y 轴向上、Qt 场景 Y 轴向下造成的整盘上下镜像，修正坐标投影并增加方向回归测试。 |
| 2026-08-04 | `0.4.2` 斜向标签修复 | 同步交换镜像后的左右斜向标签族，保留原版约束射线不变；Hard seed 1 的 `-4-`、`-5-`、`5` 标签斜率与官方画面重新一致。 |
| 2026-08-04 | 一键启动与首次 GitHub 发布 | 新增 `启动求解器.cmd`、环境/依赖自修复、托管核心构建和启动前诊断。提交 `96813b2`、`0527a26` 已推送到 `agent/offline-generator-and-launcher`，Draft PR #1 用于合并到远端 `main`。 |
| 2026-08-05 | 开发空间整理 | 将规则与路线资料纳入仓库；加入可公开的 Easy/Hard seed 1 TSV 夹具和最终 UI 证据；标准单元测试不再依赖工作区外的 `reverse_harness/exports`。重复截图、中间构建和失效同步脚本转入本地日期归档。 |
| 2026-08-05 | `0.4.3` 详细可核查推理解释 | 将局部计数、排列、子集差分、全盘剩余和 CP-SAT 全局步骤统一改成分段推导；全局反证提取足以无解的关键约束，界面改用可滚动说明区，并新增对应分支与长文本回归测试。 |
| 2026-08-05 | 暂时关闭截图识别入口 | 由于现有截图识别精度仍不足，禁用界面截图按钮，并在处理函数增加关闭保护；保留原 OCR 代码和研究资料，等待“识图到地图”转换逻辑重做后再开放。 |
| 2026-08-05 | `0.5.0` Windows 单文件发布 | 不修改现有 UI 与求解核心，增加冻结环境资源定位、PyInstaller 单文件构建、版本/图标资源、专有 DLL 排除检查和成品级 UI + Easy/Hard seed 1 冒烟测试；Release 提供 EXE 与 SHA-256。 |
| 2026-08-05 | `0.6.1` 推理阅读与行线索可见性 | 扩大推理原因显示区域，删除步骤历史展示，强制所有行线索常显并避开覆盖控件；保留底层撤销记录，求解器与生成器核心不变。 |
| 2026-08-05 | `0.6.2` 侧栏精简与完整推理显示 | 将长推理正文与底部操作栏严格分离，删除重复的“剩余/冲突”卡片和手动标记文字，压缩图例；用 Hard seed 3 第 55 步长全局反证验证首屏、滚动与末尾完整可见。 |
| 2026-08-05 | `0.6.3` 种子缓存与设置页 | 在不修改 Easy/Hard 生成器和求解器的前提下加入版本化结果缓存；右下角增加可扩展设置页，可查看缓存统计、路径并确认删除。 |
| 2026-08-06 | `0.6.4` 全屏推理末行显示修复 | 根据真实全屏截图定位滚动条到底后末行仍被文本视口裁切的问题；改用文档根帧安全尾距并增加末尾光标可见性回归。 |
| 2026-08-06 | 仓库收敛到最新版本 | 将 0.6.4 最新源码归并到 `main`，删除旧版 UI 截图、本地旧安装包、GitHub 旧 Release/标签和已验证无回滚价值的日期归档；保留完整源码提交历史、版本演进文档、OCR 数据与原版差分环境。 |
| 2026-08-06 | `0.6.5` 手动标记选中轮廓 | 未知、蓝格、排除三个按钮的选中外轮廓分别改为黑色、橘色、蓝色；新增互斥状态和实际像素渲染回归，核心逻辑保持不变。 |
| 2026-08-07 | `0.7.0` 推理数据与棋盘动态联动 | 将解释中的单坐标、坐标数组和三方向行名称关联到棋盘；悬停临时预览、点击固定并加粗文字，长数组整体联动，同时保持求解解释原文和核心算法不变。 |
| 2026-08-07 | `0.7.1` 提示框清理与高亮视觉优化 | 彻底取消推理引用悬停提示框；将单层粗描边改成柔光与精细轮廓组成的分层高亮，并把持续呼吸收敛为一次性淡入。 |
| 2026-08-07 | `0.7.2` 全局唯一性分层门禁 | 将逐步求解改为全场按难度逐层短路；更简单推理存在时不再计算复杂层级，全部局部层级耗尽后才进入 CP-SAT 全局反证。 |
| 2026-08-07 | `0.7.3` 启动方式与使用说明 | 移除应用建议按钮的悬停提示框；默认有窗口最大化并支持三种持久化启动方式；以可关闭、可重开的四步手绘说明替代启动模拟盘面。 |
| 2026-08-08 | `0.7.4` 启动窗口下拉选择器 | 将受系统主题影响的深色常驻列表改为默认折叠的白底选择器；增加浅蓝选中态、明确箭头、焦点轮廓和真实渲染回归。 |
| 2026-08-14 | `0.8.0` 使用连续性与响应体验 | 将下一步推理移入后台线程；加入过期结果保护、安全退出、撤销/重做、版本化自动保存、启动恢复和 `.hexsave` 手动存档管理。 |

## 版本演进

### `0.1.0`：截图/OCR 中文求解助手

- 从完整游戏截图检测橙、蓝、黑、灰六边形。
- OCR 识别格内数字、外侧行线索和剩余数，并允许人工补录。
- 支持局部计数、连续/非连续排列、子集差分和 CP-SAT forced-move 检查。
- 证明“抽象盘面 + 精确约束 + 中文解释”的路线可行，但 OCR 准确率限制了实用性。

### `0.2.x`：种子解题台与原生棋盘

- 将主工作流改为种子、难度、当前玩家进度和单步建议。
- 将公开盘面与私有生成答案隔离；私有答案只参与测试核对，不参与求解。
- 使用 Qt 原生几何绘制棋盘，支持缩放、平移、目标高亮和动态提示释放。

### `0.3.0–0.3.1`：原版生成器桥接

- 通过隔离运行时确认生成器入口、种子格式、三方向行线索和公开/隐藏层。
- Easy、Hard 都能由同一个八位十进制种子重建。
- 建立 `schema -> parse -> board -> solve -> private-answer replay` 的验证闭环。

### `0.4.0`：离线精确生成

- Hard：纯 Python 重放形状、颜色、数字、花格、行线索、可解性校验和冗余线索裁剪。
- Easy：无 Unity 进程的托管宿主执行 `OldLevelGenerator + MarvinHexcellsSolver`。
- 原版隔离副本降为显式差分工具，默认运行不再启动游戏。

### `0.4.1–0.4.2`：官方显示方向一致

- 修正棋盘 Y 轴投影。
- 修正左右斜向标签镜像。
- 坐标、约束射线和显示标签分别建模，避免视觉修复改变生成内容。

### `0.4.3`：详细可核查推理解释

- 普通局部规则显示线索要求、已知蓝格、未知格、还需数量、坐标集合和计算式。
- 连续/非连续规则显示合法排列数量、排列样例及目标格在所有排列中的固定值统计。
- 子集规则展开两个条件的需求计算、包含关系、差集和需求差。
- 全局规则对错误颜色做反证，列出由 CP-SAT assumptions 返回的一组充分冲突条件，并再次验证相反颜色仍有可行解。
- 说明区改为可滚动只读文本，保留完整多行理由且不挤掉“计算/应用”按钮。

### `0.5.0`：Windows 单文件应用

- 将原有 PySide6 应用冻结为一个 Windows x64 EXE；用户无需安装 Python、Conda 或依赖。
- 保持 `MainWindow`、主题、控件尺寸、交互信号和求解/生成代码路径不变，只增加冻结资源定位和打包入口。
- 成品内置项目自有的 Easy 无 Unity 托管宿主及兼容 DLL，不包含原版 `Assembly-CSharp.dll`、游戏 EXE 或存档。
- 增加 `--package-smoke-test`：由成品自身创建真实窗口，核对标题、最小/默认尺寸、侧栏宽度与截图关闭状态，并完成 Easy/Hard seed 1 生成和第一步校验。
- 构建脚本输出单文件 EXE 与无 BOM SHA-256 文件，并检查 PyInstaller 归档中的必需文件和专有文件边界。

### `0.6.1`：推理阅读与行线索可见性

- 删除左侧“步骤历史”面板，让“下一步”面板使用侧栏剩余高度；底层 `InteractivePuzzleSession.history` 继续服务于撤销功能。
- 推理原因文本区最小高度提高到 220 px，取消最大高度，字号提高到 13 px；默认窗口可获得约 487 px 的实际高度，最小窗口仍不少于 220 px。
- 行线索统一跟踪并在每次盘面同步时恢复 `visible=true`、不透明度 1 和前景层级；棋盘 viewport 为顶部、右侧和工具栏预留安全边距。
- 增加源码与成品级回归检查，确认历史控件不存在、长解释可滚动、推理区高度符合预期、全部行线索持续可见。
- 本版不修改求解器、CP-SAT、生成器、种子一致性或私有答案隔离逻辑。

### `0.6.2`：侧栏精简与完整推理显示

- 删除左侧重复的“剩余/冲突”统计卡，剩余数继续由棋盘右上角六边形计数器显示。
- 手动标记按钮从 `76 × 88` 缩小到 `60 × 56`，六边形半径从 25 缩小到 20，删除按钮下方文字；tooltip、无障碍名称和无障碍说明继续提供语义。
- 推理正文使用独立、不透明、可滚动的文本区域，底部增加固定 46 px 操作栏；两者是布局中的兄弟区域，不再依赖覆盖或透明背景。
- 默认窗口推理正文实际高度不少于 560 px，`1120 × 760` 窗口不少于 300 px；自动测试同时验证正文几何范围严格位于操作栏上方且能滚动到最大值。
- 精确回放 Hard seed `00000003`：先应用 54 步，再显示第 55 步约 1709 字的“全局求解”理由；`1440 × 1024` 首屏、`1120 × 760` 首屏和最小窗口滚动末尾均通过视觉检查。
- 截图 QA 工具的 Hard 生成等待时间改为可配置，默认 180 秒，并支持直接滚动到推理末尾；该工具改动不影响产品运行路径。
- 成品复构时发现一次剩余计数器重绘崩溃：删除手动按钮文字绘制代码时误删了同文件中计数器仍需的 `QFont` 导入；恢复导入并增加计数器实际渲染回归检查。
- 本版不修改求解器、CP-SAT、生成器、种子一致性或私有答案隔离逻辑。

### `0.6.3`：种子结果缓存与设置管理

- 默认 Easy/Hard 注册表启用自动持久缓存；首次请求仍运行对应精确后端，再次请求相同 Steam Build、后端版本、难度和种子时直接读取本地结果。
- 缓存键同时包含 schema、Build、难度、八位种子、后端 ID 与精确性，避免不同算法或游戏版本之间误用旧结果。
- 缓存载荷为显式字段序列化、大小受限且完整校验的 JSON，不执行 Pickle 或任意代码；公开盘面、私有答案和揭示元数据恢复后仍执行答案完整性检查。
- 写入采用缓存目录内临时文件、刷新落盘和 `os.replace` 原子替换；损坏 JSON、门禁不匹配、读取失败或写入失败时自动回退到精确生成器。
- 默认目录为 `%LOCALAPPDATA%\HexInfiniteSolver\seed-cache\v1`；测试和成品冒烟可通过 `HEXSOLVER_CACHE_DIR` 使用独立临时目录，不污染普通用户缓存。
- 右下角工具栏增加齿轮入口；设置页采用可滚动分区布局，当前“种子结果缓存”卡片显示启用状态、条目数、占用空间和实际路径，并提供带二次确认的删除按钮。
- 缓存命中在棋盘状态中明确显示“本地缓存”，首次成功显示“离线精确生成”并提示已缓存，避免把复用结果冒充本次重新计算。
- 新增缓存隔离、失效、损坏重建、写入失败、精准清理和设置页回归测试；求解器、CP-SAT、Easy/Hard 生成算法与私有答案使用边界均未修改。

### `0.6.4`：全屏长推理末尾安全区

- 用户全屏截图显示：推理滚动条已到最底部，但最后一句仍只有上半部分可见；按钮没有覆盖 `QTextEdit` 控件几何，真正缺少的是文本文件内部的可滚动尾距。
- 删除 `QTextEdit.setViewportMargins()` 和样式表中重复的底部 padding，避免 Qt 文本视口、滚动范围和外部操作栏之间出现不同的底边计算。
- 每次设置推理正文后，在 `QTextDocument` 根帧写入 28 px `bottomMargin`；它会计入文档滚动高度，但不会改变 `toPlainText()`，因此显示和复制的解释原文都不会被补入空行。
- 回归测试在 80 行长文本中滚动到最大值，读取文档末尾光标矩形，要求其完整位于视口底边至少 16 px 以上，并同时验证根帧尾距存在。
- 使用 Hard seed `00000003` 第 55 步重新回放，在与用户全屏截图同类的宽屏布局中滚动到底；“最小的证明。”完整显示，正文与固定按钮栏之间保留可见空隙。
- 成品冒烟加入同样的长文本末尾光标检查，避免源码版正常、冻结成品再次退化；求解器、生成器、缓存、设置页和当前局面数据均未修改。

### `0.6.5`：手动标记选中轮廓与原版式鼠标操作

- `StateButton` 保留橙、蓝、深灰三种状态填充，未选中时继续使用白色边缘，不改变按钮尺寸、布局、提示或无障碍名称。
- 选中未知时使用深黑轮廓，选中蓝格时使用橘色轮廓，选中排除时使用蓝色轮廓；三种颜色都与各自填充形成清晰对比。
- Qt 回归会依次真实点击三个按钮，核对互斥选中状态，并统计按钮位图中目标轮廓颜色的实际像素，防止只修改常量却没有绘制出来。
- 冻结成品冒烟执行同样检查后再恢复默认未知状态；求解器、生成器、种子缓存、推理文档和私有答案隔离均未修改。
- 设置页新增默认关闭、立即持久化的“原版左右键操作”：开启后左键排除、右键标记蓝色；对同一状态再次按相同按键会恢复未知。
- 原版式操作开启时禁用三种手动状态按钮并更新帮助提示；关闭后恢复原有“选择状态再左键点击”的交互，避免两套输入规则同时生效。
- Qt 回归使用真实视口鼠标事件验证默认模式、左右键映射、重复点击复原、设置持久化和工具禁用状态；冻结成品冒烟执行同一条设置与操作链路。

### `0.7.0`：推理数据与棋盘动态联动

- 新增独立 `reason_interaction.py`，只依赖解释原文和当前公开 `Board`：先识别完整坐标数组，再识别 `RowClue.display_name()`，最后处理单坐标，避免长数组被拆成多个竞争链接。
- 坐标引用只保留当前棋盘实际存在且非 `OUTSIDE` 的格子；不存在坐标被安全忽略。行名称直接使用模型显示名匹配，因此横向、左下斜、右下斜继续服从 `0.4.2` 已修正的镜像标签。
- 推理正文从普通只读文本框升级为保持 `toPlainText()` 完全一致的富文本浏览器；引用不加下划线，悬停用浅青背景，点击固定后加粗，Enter/空格提供等价键盘操作。
- 棋盘增加独立的引用覆盖层：悬停为低幅透明度呼吸的虚线轮廓，点击固定后为静态实线；成组坐标一次性显示，行引用同时改变外侧线索字色和字重。覆盖层不修改格子填充、目标格或手动选中描边。
- 系统样式关闭动画或设置 `HEXSOLVER_REDUCED_MOTION=1` 时自动停用呼吸效果；点击固定仍提供静态实线和文字字重反馈，不只依赖颜色。
- 应用下一步、手动改格、撤销、重置、换步骤或换棋盘都会清理临时/固定引用，防止旧解释继续指向新局面。
- 新增解析器、Qt 悬停、行联动、坐标组联动、固定/取消、原文保持和状态清理回归；成品冒烟直接检查富文本锚点、棋盘覆盖层和行线索映射。
- 本版没有修改 `HexReasoningSolver` 的规则、解释生成文本、Easy/Hard 生成器、种子缓存或私有答案隔离。

### `0.7.1`：提示框清理与高亮视觉优化

- 删除推理引用字符格式的 ToolTip，并在正文视口层拦截所有 `ToolTip` 事件，避免系统主题异常时出现白底白字或遮挡解释的浮层。
- 每个关联格新增外层柔光多边形，原覆盖层收窄为精细状态线；两层均位于格子内容之外，不改变橙/蓝/黑填充、白色基础边缘、目标格或手动选中描边。
- 悬停态使用亮青细虚线与低透明柔光，固定态使用深青实线与静态柔光；虚实线、文字字重和点击固定共同提供非颜色状态差异。
- 悬停柔光仅在约 240 ms 内缓出淡入一次，随后停止计时器；系统减少动态或 `HEXSOLVER_REDUCED_MOTION=1` 时直接显示最终静态效果。
- 保留行线索与覆盖格同步、长坐标数组成组联动、点击/键盘固定、换步骤及盘面操作时清理旧状态的全部行为。
- 新增无提示框、双层几何、虚实线、有限动画和减少动态测试；本版仍未修改求解结论、解释原文、Easy/Hard 生成器、缓存或私有答案隔离。

### `0.7.2`：全局唯一性分层门禁

- 评估确认旧版已经不会在“任意局部候选完全为空”之前调用全局求解，但每一步仍会把局部计数、排列、子集差分和全场剩余全部计算完再排序。
- 新版 `next_step()` 先扫描全场局部计数；无结果再依次进入排列、子集差分和全场剩余，四层全部为空后才调用 CP-SAT 全局唯一性反证。
- 每层内部继续使用稳定坐标顺序。严格简单优先可能改变合法步骤的先后与理由来源，但每一步仍必须满足公开约束并与测试私有答案一致。
- `solve()` 继续保留原有批量候选收集语义；规则实现、全局模型、解释模板、地图生成、UI、缓存和私有答案隔离均未修改。
- 新增局部计数短路、排列短路和“全部局部层完成后才调用全局”的回归测试，并完整回放 Easy/Hard seed 1 到零未知格。

### `0.7.3`：启动方式与使用说明

- “应用当前建议”按钮不再设置 ToolTip，因此悬停时不会出现“把建议应用到本地盘面”浮层；按钮仍直接应用当前必然步，并保留无障碍名称和说明。
- 新增持久化启动窗口模式：默认有窗口最大化，另可选择无边框全屏或普通窗口；设置立即保存，但只在下次启动时改变窗口状态。
- 主窗口初始状态改为空 `Board`，不再把开发演示盘当成启动内容。盘面、手动标记、下一步和棋盘工具在没有真实盘面时保持禁用。
- 启动层使用 Qt 原生几何绘制双线手绘箭头和圈点目标，并用四个真实文本控件说明种子生成、手动同步、下一步推理和设置入口；不引入额外位图资源。
- 使用说明可直接关闭；成功生成真实种子盘面后自动收起。设置页提供“重新查看使用说明”，打开说明时暂停棋盘操作，关闭后恢复当前真实盘面。
- 本版只调整启动、设置和引导交互；`HexReasoningSolver`、解释模板、Easy/Hard 生成器、种子缓存格式和私有答案隔离均未修改。

### `0.7.4`：白底启动窗口下拉选择器

- 根据真实 Windows 设置页截图确认，原控件本体虽指定白底，弹出的 `QAbstractItemView` 仍会继承系统深色表面，展开后产生一块几乎不可读的深色区域。
- 启动模式控件改为默认折叠的标准下拉选择器；关闭时只显示当前值，展开后显示有窗口最大化、无边框全屏和普通窗口三项。
- 控件与弹出列表同时设置 QSS 和 `QPalette`，使用白色表面、深炭文字、浅蓝悬停/选中态；自定义简洁下拉箭头和蓝色键盘焦点轮廓，保持原有橙、青、炭灰视觉语言。

### `0.8.0`：后台推理与局面连续性

- “计算下一步”改由独立 `QThread` 在公开盘面深拷贝上执行；按钮显示计算中状态，主界面保持响应，核心推理调度和结论未修改。
- 每次改盘、撤销、重做、重置或换图都会递增局面版本并请求中断旧推理；线程返回时再次比对版本，因此旧建议不会落到新盘面。关闭窗口时不强杀求解线程，而是请求取消并在安全边界结束后自动关闭。
- `InteractivePuzzleSession` 增加重做栈；撤销会把完整格子状态和揭示线索压入重做栈，重做后恢复历史，新操作和重置会清除旧分支。工具栏增加重做按钮，并提供 `Ctrl+Z` / `Ctrl+Y`。
- 新增 `SessionStore`：自动与手动存档均采用带 schema、应用版本、游戏 Build 和 SHA-256 完整性字段的 JSON，写入使用同目录临时文件、刷新落盘和原子替换；载入时从初始盘面回放历史并验证重做链路。
- 最近一次真实局面默认自动保存；下次启动询问恢复或放弃，损坏、篡改、超限和不兼容文件都安全拒绝。设置页可关闭启动询问、手动保存/载入 `.hexsave`，或只清除当前局面和自动存档。
- 原生 Windows Qt 截图确认新设置卡保持白底、橙色图标、青色状态和清晰禁用态；自动测试覆盖后台线程、过期结果、恢复接受/放弃、存档损坏和撤销/重做。
- 回归测试真实展开和收起下拉菜单，核对白底调色板与实际像素比例，并把同样的检查加入冻结成品冒烟。
- 三种启动模式的值、持久化和下次启动应用逻辑保持不变；求解、解释、地图生成、缓存格式和私有答案隔离均未修改。

## 关键技术判断

1. **种子生成优先于 OCR。** 已知种子时直接重建结构化盘面，可靠性和解释能力都优于截图识别；当前截图入口已暂时关闭，OCR 代码只作为待重做能力保留。
2. **生成器和求解器分离。** 生成器产生公开盘面、私有答案和揭示元数据；求解器只读取当前公开状态。
3. **先简单、后复杂，最后全局。** 按局部计数、排列、子集差分和全场剩余逐层扫描；只有这些层级全部停住时才调用 CP-SAT 假设反证。
4. **内容差异和投影差异分开验证。** 先比较 TSV/对象，再比较屏幕坐标，避免把镜像问题误判为 RNG 或生成算法错误。
5. **原游戏保持只读。** 安装目录和存档不由本项目修改；需要启动原版做差分时，只使用本地隔离副本。

## 逆向确认的生成链路

- Hard：`LevelGenerator` 选择五类形状之一，随后执行格子与数字生成、简单解裁剪、行线索生成、`TryToSolve` 和冗余线索裁剪。
- Easy：`OldLevelGenerator` 使用独立的参数和随机调用顺序，并由 `MarvinHexcellsSolver` 验证。
- 两套生成器都接受八位十进制种子，但不是同一套算法。
- Hard 的原版内部求解器是生成阶段的 depth-2 验证器；应用中的 CP-SAT 全局反证是另一个独立求解后端。

## 验证记录

- Easy/Hard seed 1–50 与原版最终 TSV 逐字段、逐对象、逐顺序一致。
- `00000000`、`99999999` 两个边界种子的 Easy/Hard 输出一致。
- Hard 样本覆盖 Columnar、Diamond、Triangle、Hex、Hourglass 五种形状。
- 代表种子能够完整逐步回放到零未知格，每一步与私有答案一致。
- 当前自动测试为 84 项；有原版程序集时会额外执行 Easy 托管核心的真实集成校验，否则该项明确跳过。
- 启动器自检会验证托管核心、原版程序集哈希和默认后端不包含游戏进程。
- 0.5.0 发布前额外执行单文件归档检查和成品级 UI + Easy/Hard seed 1 冒烟测试；构建命令成功本身不算成品验证。
- 0.5.0 最终 Windows x64 成品为 96,461,270 字节（91.99 MiB），SHA-256 为 `2628f23b6e11888e6613aa64d654d9016c1679c7ed8cd59af7c7e9c24c1db7da`。
- 0.6.1 发布前通过 45 项自动测试、一键启动器检查、Easy/Hard seed 1 离线诊断和真实成品冒烟测试。
- 0.6.1 最终 Windows x64 成品为 96,459,453 字节（91.99 MiB），SHA-256 为 `01d8655f78d2aca7091ff60c247312421b7189cce6e8546e37a6ca8196971b28`。
- 0.6.2 发布前通过 45 项自动测试、一键启动器检查、Easy/Hard seed 1 离线诊断、Hard seed 3 第 55 步长解释视觉回放和真实成品冒烟测试。
- 0.6.2 最终 Windows x64 成品为 96,460,100 字节（91.99 MiB），FileVersion/ProductVersion 均为 `0.6.2`，SHA-256 为 `ff8768de83158a22f622bfa357fea8535cc5a689375cb04265fea555cfa417ed`。
- 0.6.3 发布前通过 53 项自动测试、一键启动器检查、Easy/Hard seed 1 首次生成与再次缓存命中验证、设置页视觉检查和真实成品冒烟测试。
- 0.6.3 最终 Windows x64 成品为 96,481,831 字节（92.01 MiB），FileVersion/ProductVersion 均为 `0.6.3`，SHA-256 为 `fb9a7e721c1d56f8266c6e2206fdd5f83bf43f30ffe22cadeadc6cb35967ea58`。
- 0.6.4 发布前通过 53 项自动测试、一键启动器检查、Hard seed 3 全屏类长解释末尾视觉回放，以及包含末尾光标安全区、设置页和 Easy/Hard 缓存命中的真实成品冒烟测试。
- 0.6.4 最终 Windows x64 成品为 96,482,715 字节（92.01 MiB），FileVersion/ProductVersion 均为 `0.6.4`，SHA-256 为 `511c62826d59996ac68cb93450f639f9d0ec563fa5003661d8ec44c6fefa2783`。
- 0.6.5 发布前通过 56 项自动测试、一键启动器检查、三种手动标记选中状态和原版式左右键操作的 Windows 实际渲染，以及包含轮廓像素、鼠标设置、长解释末尾安全区、Easy/Hard 缓存命中的真实成品冒烟测试。
- 0.6.5 最终 Windows x64 成品为 96,486,810 字节（92.02 MiB），FileVersion/ProductVersion 均为 `0.6.5`，SHA-256 为 `c7043d7c5ada00d7915a912f2025fb8cf653064cf81919ecc07944e85604b45a`。
- 0.7.0 发布前通过 65 项自动测试、两张真实 Windows Qt 联动截图，以及包含富文本引用、行/坐标组覆盖层、状态清理、长解释末尾安全区、鼠标设置和 Easy/Hard 缓存命中的真实成品冒烟测试。
- 0.7.0 最终 Windows x64 成品为 96,503,533 字节（92.03 MiB），FileVersion/ProductVersion 均为 `0.7.0`，SHA-256 为 `d0d93db892ad62141b5053f9519326baaea7e19971bd53d50619835b8e290fa6`。
- 0.7.1 发布前通过 66 项自动测试、两张真实 Windows Qt 分层高亮截图，以及包含无提示框字符格式、双层覆盖、虚实线状态、清理、长解释末尾安全区、鼠标设置和 Easy/Hard 缓存命中的真实成品冒烟测试。
- 0.7.1 最终 Windows x64 成品为 96,504,408 字节（92.03 MiB），FileVersion/ProductVersion 均为 `0.7.1`，SHA-256 为 `a62868ea1b7b8498f4809f9bd74dbf4bf28e9cdb4205ba1e5b2757f1594a5abc`。
- 0.7.2 发布前通过 69 项自动测试、三类推理层级门禁回归，以及 Easy/Hard seed 1 完整逐步回放；两张盘均解到零未知格并逐步匹配私有答案，Hard 的全局求解调用仍为 10 次，Easy 仍为 0 次。
- 同进程基线对照中，Easy seed 1 完整回放由 8.58 秒降至 0.20 秒，Hard seed 1 由 6.62 秒降至 5.98 秒；这是代表夹具上的单次开发机测量，不作为所有设备与种子的固定性能承诺。
- 0.7.2 最终 Windows x64 成品为 96,501,119 字节（92.03 MiB），FileVersion/ProductVersion 均为 `0.7.2`，SHA-256 为 `0ee95231593f1d7b3be63cad51076c2d37718048f19bec87db1bee6f6b758741`。
- 0.7.3 发布前通过 73 项自动测试、两张 `1440 × 1024` 原生 Windows Qt 视觉检查，以及覆盖默认最大化、空盘启动、四步说明、三种窗口设置持久化、说明重开、真实盘面自动收起、无应用建议提示框和原有 Easy/Hard 缓存链路的冻结成品冒烟测试。
- 0.7.3 最终 Windows x64 成品为 96,512,704 字节（92.04 MiB），FileVersion/ProductVersion 均为 `0.7.3`，SHA-256 为 `baf45bf71b9c556a3c1734ce480168c98e7e487a939dcc1dbc635c22a2f8ead1`。
- 0.7.4 发布前通过 74 项自动测试、折叠与展开两种原生 Windows Qt 视觉检查，以及覆盖白底调色板、浅蓝当前项、实际像素比例、展开/收起和原有 Easy/Hard seed 1 缓存链路的冻结成品冒烟测试。
- 0.7.4 最终 Windows x64 成品为 96,518,191 字节（92.05 MiB），FileVersion/ProductVersion 均为 `0.7.4`，SHA-256 为 `1d14b16aa9948b71dc8d66fcf44dc4c4598d47402f46e97b0e4013de90ab908d`。
- 0.8.0 发布前通过 84 项自动测试和原生 Windows Qt 设置页视觉检查；冻结成品继续核对 Easy/Hard seed 1、缓存、重做和自动存档恢复。
- 0.8.0 最终 Windows x64 成品为 96,539,892 字节（92.07 MiB），FileVersion/ProductVersion 均为 `0.8.0`，SHA-256 为 `48570b61db4ecc64ce9b33932747fe8f3d02eb96599fd19a8d09c499a7d0d004`。

## 当前边界与下一步

- Easy 仍需要用户合法安装版本中的 `Assembly-CSharp.dll`，仓库不会分发该文件。
- Hard 复杂种子忠实重放原版多轮求解与裁剪，少数种子可能需要几十秒。
- CP-SAT 全局步骤已提取足以造成无解的关键冲突条件，但它不是完整人工证明树，也不保证是数学上的最小或唯一冲突核心。
- 0.8.0 单文件成品未做商业代码签名，首次下载可能触发 Windows SmartScreen；Release 提供 SHA-256 供核验。
- 后续优先项见 [`ROADMAP.md`](ROADMAP.md)：全局求解性能、长线索局部推理、诊断、导入导出和稳定发布。

## 文档与证据入口

- 项目说明：[`README.md`](README.md)
- 工作区边界：[`docs/WORKSPACE.md`](docs/WORKSPACE.md)
- 求解算法：[`docs/solver/ALGORITHM.md`](docs/solver/ALGORITHM.md)
- 种子开发清单：[`docs/generator/IMPLEMENTATION_PLAN.md`](docs/generator/IMPLEMENTATION_PLAN.md)
- 设计验收：[`docs/design/QA.md`](docs/design/QA.md)
- 历史研究资料：[`docs/research`](docs/research/README.md)
