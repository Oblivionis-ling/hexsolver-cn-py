# Hexcells 中文求解器技术路线

版本基线：`0.1.0`

这份文档用于重新约定项目路线。当前项目不再追求复刻原 `HexSolver` 的窗口自动点击能力，而是先把“截图导入、OCR 辅助、人工校对、求解解释、截图叠图”做成稳定的中文工作台。

## 1. 项目定位

当前阶段的目标是：

- 用户导入一张 `Hexcells Infinite` 截图。
- 程序自动识别棋盘网格、已知格、外侧线索和 `REMAINING`。
- OCR 不确定时，界面提供足够顺手的人工校对入口。
- 求解器只输出当前必然成立的步骤，并给出中文理由。
- 求解建议直接叠加到截图上，方便对照游戏操作。

暂时不做：

- 直接捕捉游戏窗口。
- 自动点击游戏。
- 训练专用深度 OCR 模型。
- 一次性生成完整人类证明树。

## 2. 推荐架构

### 2.1 桌面 UI

继续使用 `CustomTkinter`。

理由：

- 当前项目已经跑通。
- 打包和部署比 WebView / Qt 更轻。
- 对这个阶段的“导入截图 + 校对表格 + 大图叠层”足够。

后续如果 UI 复杂度继续上升，可以考虑迁移到 `PySide6 + QGraphicsView`。迁移触发条件是：

- 需要更强的画布交互。
- 需要复杂的图层管理。
- 需要更专业的表格编辑体验。

### 2.2 图像识别

棋盘几何识别继续用 `OpenCV`。

当前路线：

- 用橙色未知格检测推断六边形网格。
- 根据网格反推出所有可玩格坐标。
- 用颜色统计识别未知格、蓝格、黑格、灰格。
- 用整图 OCR 框和局部模板 OCR 共同判断文本。

下一阶段重点：

- 行线索 anchor 的几何校准。
- 三个方向线索的专门裁剪窗口。
- 对 OCR 框和行线索做更可靠的一对一匹配。

### 2.3 OCR

继续使用 `RapidOCR + 模板 OCR` 的混合路线。

分工：

- `RapidOCR`：整图找文本框，负责中文 UI 标签、顶部 `REMAINING`、外侧线索候选。
- 模板 OCR：对游戏字体的数字、`{n}`、`-n-` 做精细匹配。
- 人工检查台：处理 OCR 不确定项，不把低可信结果强行写入约束。

原则：

- 宁可少自动填，也不要把错线索塞进求解器。
- 自动确认只适用于高置信候选。
- 所有 OCR 候选都要保留来源、分数、框坐标，便于 UI 回看。

### 2.4 求解器

继续使用 `OR-Tools CP-SAT` 做全局约束求解，局部推理放在前面。

流程：

- 先做局部确定性规则。
- 再做子集差分。
- 局部无结果时，使用 CP-SAT 检查 forced move。
- 每条建议必须附带中文理由。

算法细节见 [SOLVER_ALGORITHM.md](./SOLVER_ALGORITHM.md)。

### 2.5 数据和版本

当前版本号：`0.1.0`

版本文件：

- `VERSION`
- `pyproject.toml`
- `src/hexsolver_cn/__init__.py`

版本规则建议：

- `0.1.x`：OCR、UI、求解解释的小修小补。
- `0.2.0`：行线索自动识别明显提升，检查台交互稳定。
- `0.3.0`：加入测试集、回归评估和打包流程。
- `1.0.0`：截图识别、校对、求解、导出都稳定，普通用户可以连续使用。

## 3. GitHub 仓库策略

仓库命名和文件命名优先使用简单英文。README 和设计文档可以使用中文，以便当前项目沟通更清楚。

仓库里只保留：

- 源码
- 文档
- OCR 模板资源
- Python 项目配置
- 依赖说明

仓库里不保留：

- `.conda_env`
- `__pycache__`
- 导出叠图
- 临时日志和缓存
- 本机 `.cmd` 文件
- 本地仓库管理脚本

本地可以继续使用同步脚本或 Git hook 辅助开发，但这些文件不进入 GitHub 仓库。

## 4. 下一步优先级

1. 行线索 OCR 几何校准。
2. 增加测试集评估脚本，统计每张图的格子数、行线索命中、`REMAINING` 命中和求解建议数。
3. 做一个“疑似错线索”检查：求解器无解时反向提示哪些 OCR 线索最可疑。
4. 打包成单文件或安装包。
