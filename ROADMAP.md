# HexInfinite 种子求解器路线图

当前基线：`0.6.5`

完整开发清单和一致性验收门槛见 [生成器实现计划](./docs/generator/IMPLEMENTATION_PLAN.md)。本文件只保留版本级路线。

## 0.2.x：游戏风格解题台

- `PySide6 + QGraphicsView` 双栏界面。
- 种子、难度、三态手动标记、撤销、重置和步骤历史。
- 一次只显示一个必然步骤、目标格和中文理由。
- 截图/OCR 降为次级入口。
- 未通过原游戏逐格验证的种子后端一律拒绝使用。

## 0.3.0–0.3.1：Easy/Hard 原版运行时桥接（已完成）

- 方案 2 双栏游戏风格 UI。
- 隔离副本中调用 Easy/Hard 原版生成器与内置求解器。
- 最终格子、公开状态、局部提示和三方向行线索 TSV 合约。
- 后台生成、错误恢复、版本哈希门禁和真实冒烟诊断。
- 覆盖层下提示随玩家进度释放，撤销时完整恢复。
- Easy 多种子和 Hard 五种形状代表种子的完整回放；每一步核对私有答案。

## 0.4.0：默认生成无游戏进程（已完成）

- Hard 固化 Unity `5.6.3f1` 随机序列，完整移植 `LevelGenerator + Solver/Set`。
- Hard 覆盖五种形状、蓝黑分布、花格、三方向行线索、补线索和冗余裁剪。
- Easy 用最小 Unity API 兼容层直接执行 `OldLevelGenerator + MarvinHexcellsSolver`。
- 默认注册表中不再存在 `Hexcells Infinite.exe` 调用；原版桥仅用于显式差分。
- Easy seed 1/2/3 与原版最终 TSV 逐字段一致；Hard seed 1–30 全部逐字段一致并覆盖五种形状。

## 0.4.1：官方屏幕方向一致性修复（已完成）

- 修正 Unity 世界坐标 Y 轴向上、Qt 场景 Y 轴向下造成的整张地图上下镜像。
- Easy/Hard seed 1–50 重新与原版 TSV 逐行、逐对象、逐顺序核对一致。
- `00000000`、`99999999` 两个八位边界种子一致；Hard 覆盖五种形状。
- seed 1/2 与官方截图的轮廓和线索方位一致。

## 0.4.2：斜向标签镜像修复（已完成）

- 棋盘做 Unity 到 Qt 的 Y 轴镜像时，同步交换左右斜向标签。
- 约束射线仍使用原版坐标方向，只有镜像后的视觉标签族发生交换。
- 新增斜向标签族回归测试，防止标签再次指向错误的条件线。

## 0.4.3：详细可核查推理解释（已完成）

- 局部计数、排列、子集差分和全盘剩余规则均展开当前状态、坐标与计算式。
- 全局 forced move 用错误假设做反证，并提取一组足以导致无解的关键约束。
- 明确区分“足够的冲突集合”和“唯一/最小证明”，不把可行性检查夸大成人工证明树。
- 下一步说明区改为固定高度的可滚动文本视图，长解释不会挤掉操作按钮。
- 增加各推理类型的判蓝/判黑分支测试和 Qt 长文本显示测试。

## 当前临时状态：截图识别入口关闭

- 截图/OCR 代码暂时保留，但界面入口与处理函数均已加关闭保护。
- 当前稳定入口是“种子生成 + 手动同步局面”。
- 待识图到地图的转换逻辑重做并完成代表截图精度验证后，再重新开放入口。

## 0.5.0：Windows 单文件应用（已完成）

- 使用 PyInstaller 将现有 PySide6 UI 和离线求解运行时封装为 Windows x64 单文件 EXE。
- UI、交互、生成器、求解器和私有答案隔离逻辑保持不变。
- 内置可再分发的 Easy 托管宿主；原版 `Assembly-CSharp.dll` 继续从本机 Steam 安装只读加载，不进入 Release。
- 成品自检会真实创建窗口，并分别生成 Easy/Hard seed 1、核对第一条建议。
- Release 同时提供 EXE 与 SHA-256 校验文件。

## 0.6.1：推理阅读与行线索可见性（已完成）

- 删除界面的步骤历史展示，但保留会话内部撤销记录和撤销功能。
- “下一步”面板占据侧栏剩余空间，推理原因区取消最大高度并提高最小高度与字号。
- 所有行线索强制保持可见并位于棋盘格上层；棋盘为顶部状态、右侧计数器和工具栏预留安全边距。
- 求解器、生成器、私有答案隔离和截图入口状态均不变。

## 0.6.2：侧栏精简与完整推理显示（已完成）

- 将推理正文滚动区与固定操作栏拆成独立布局区域，正文不会再进入或被按钮区域遮挡。
- 默认窗口的推理正文高度不少于 560 px，`1120 × 760` 窗口仍不少于 300 px，并可滚动到解释末尾。
- 删除侧栏重复的“剩余/冲突”统计卡；剩余数继续显示在棋盘右上角六边形计数器中。
- 手动标记图例缩小为 `60 × 56`，删除按钮下方文字，保留悬停提示和无障碍名称。
- 求解器、生成器、私有答案隔离、行线索常显和截图入口状态均不变。

## 0.6.3：种子缓存与设置页（已完成）

- Easy/Hard 精确生成成功后自动缓存完整结构化结果；相同游戏 Build、后端版本、难度和种子再次请求时直接加载。
- 缓存采用带 schema、Build、后端和精确性门禁的 JSON，不使用 Pickle；损坏、过期和写入失败均自动回退到精确生成器。
- 缓存写入采用同目录临时文件与原子替换，私有答案仍只供揭示和测试，不进入求解器输入。
- 右下角工具栏增加设置入口；滚动设置页当前提供缓存状态、条目数、占用空间、存储路径和带确认的删除操作，结构可继续加入其他设置。
- 成品冒烟测试增加设置页、Easy/Hard 首次写入和第二次缓存命中验证；求解器与两套生成算法均未修改。

## 0.6.4：全屏长推理末尾显示修复（已完成）

- 修复全屏窗口中推理滚动条已经到底、最后一行仍被文本视口截断的问题。
- 移除 `QTextEdit` 视口与样式表中重复的底部内边距，改为在文本根帧增加 28 px 可滚动安全尾距。
- 长解释原文保持不变，复制文本不会多出用于占位的空行。
- 回归测试直接把光标移动到文档末尾，确认末行完整位于可见视口内并与固定操作栏保持至少 16 px 间距。
- 用 Hard seed 3 第 55 步在全屏布局滚动到底重新截图验证；求解器、生成器、缓存和设置页逻辑均未修改。

## 0.6.5：手动标记选中轮廓（已完成）

- 三个紧凑手动标记按钮保留原有填充和尺寸，未选中时继续使用白色细轮廓。
- 选中未知、蓝格、排除时，外轮廓分别改为黑色、橘色、蓝色，避免所有状态共用同一青色选中提示。
- 源码测试和冻结成品冒烟均依次点击三个按钮，检查互斥选中状态、颜色映射和实际渲染像素。
- 求解器、生成器、缓存、推理显示、行线索和私有答案隔离逻辑均未修改。

## 0.6.x 后续：性能与大样本一致性

- 优化 Hard 复杂种子的 `Solver/Set` 与冗余线索裁剪性能。
- Easy 扩展到至少 30 个种子逐格、逐顺序、逐线索 100% 一致。
- 评估将 Easy Marvin 从无 Unity 托管核心继续移植为纯 Python 的收益与维护成本。

## 0.7.0：可用性与诊断

- 重做、进度保存/加载和冲突定位。
- `.hexcells` / JSON 导入导出。
- 生成器版本、游戏 Build、程序集哈希和一致性报告。
- 生成器诊断报告和已发布成品的自动更新策略。

## 1.0.0：稳定发布

- Easy/Hard 全支持。
- 种子回归、完整求解回放、私有答案隔离和打包运行全部通过。
- 普通用户无需接触游戏文件或命令行。
