# HexInfinite 种子求解器路线图

当前基线：`0.9.0`

完整开发清单和一致性验收门槛见 [生成器实现计划](./docs/generator/IMPLEMENTATION_PLAN.md)。本文件只保留版本级路线。

## 0.2.x：游戏风格解题台

- `PySide6 + QGraphicsView` 双栏界面。
- 种子、难度、三态手动标记、撤销、重置和步骤历史。
- 一次只显示一个必然步骤、目标格和中文理由。
- 截图/OCR 降为次级入口。
- 未通过原游戏逐格验证的种子后端一律拒绝使用。

## 0.3.0–0.3.1：Easy/Hard 原版运行时桥接（已完成）

- 方案 2 双栏游戏风格 UI。
- 隔离副本中调用 Easy/Hard 原版生成器与内置求解器。
- 最终格子、公开状态、局部提示和三方向行线索 TSV 合约。
- 后台生成、错误恢复、版本哈希门禁和真实冒烟诊断。
- 覆盖层下提示随玩家进度释放，撤销时完整恢复。
- Easy 多种子和 Hard 五种形状代表种子的完整回放；每一步核对私有答案。

## 0.4.0：默认生成无游戏进程（已完成）

- Hard 固化 Unity `5.6.3f1` 随机序列，完整移植 `LevelGenerator + Solver/Set`。
- Hard 覆盖五种形状、蓝黑分布、花格、三方向行线索、补线索和冗余裁剪。
- Easy 用最小 Unity API 兼容层直接执行 `OldLevelGenerator + MarvinHexcellsSolver`。
- 默认注册表中不再存在 `Hexcells Infinite.exe` 调用；原版桥仅用于显式差分。
- Easy seed 1/2/3 与原版最终 TSV 逐字段一致；Hard seed 1–30 全部逐字段一致并覆盖五种形状。

## 0.4.1：官方屏幕方向一致性修复（已完成）

- 修正 Unity 世界坐标 Y 轴向上、Qt 场景 Y 轴向下造成的整张地图上下镜像。
- Easy/Hard seed 1–50 重新与原版 TSV 逐行、逐对象、逐顺序核对一致。
- `00000000`、`99999999` 两个八位边界种子一致；Hard 覆盖五种形状。
- seed 1/2 与官方截图的轮廓和线索方位一致。

## 0.4.2：斜向标签镜像修复（已完成）

- 棋盘做 Unity 到 Qt 的 Y 轴镜像时，同步交换左右斜向标签。
- 约束射线仍使用原版坐标方向，只有镜像后的视觉标签族发生交换。
- 新增斜向标签族回归测试，防止标签再次指向错误的条件线。

## 0.4.3：详细可核查推理解释（已完成）

- 局部计数、排列、子集差分和全盘剩余规则均展开当前状态、坐标与计算式。
- 全局 forced move 用错误假设做反证，并提取一组足以导致无解的关键约束。
- 明确区分“足够的冲突集合”和“唯一/最小证明”，不把可行性检查夸大成人工证明树。
- 下一步说明区改为固定高度的可滚动文本视图，长解释不会挤掉操作按钮。
- 增加各推理类型的判蓝/判黑分支测试和 Qt 长文本显示测试。

## 当前临时状态：截图识别入口关闭

- 截图/OCR 代码暂时保留，但界面入口与处理函数均已加关闭保护。
- 当前稳定入口是“种子生成 + 手动同步局面”。
- 待识图到地图的转换逻辑重做并完成代表截图精度验证后，再重新开放入口。

## 0.5.0：Windows 单文件应用（已完成）

- 使用 PyInstaller 将现有 PySide6 UI 和离线求解运行时封装为 Windows x64 单文件 EXE。
- UI、交互、生成器、求解器和私有答案隔离逻辑保持不变。
- 内置可再分发的 Easy 托管宿主；原版 `Assembly-CSharp.dll` 继续从本机 Steam 安装只读加载，不进入 Release。
- 成品自检会真实创建窗口，并分别生成 Easy/Hard seed 1、核对第一条建议。
- Release 同时提供 EXE 与 SHA-256 校验文件。

## 0.6.1：推理阅读与行线索可见性（已完成）

- 删除界面的步骤历史展示，但保留会话内部撤销记录和撤销功能。
- “下一步”面板占据侧栏剩余空间，推理原因区取消最大高度并提高最小高度与字号。
- 所有行线索强制保持可见并位于棋盘格上层；棋盘为顶部状态、右侧计数器和工具栏预留安全边距。
- 求解器、生成器、私有答案隔离和截图入口状态均不变。

## 0.6.2：侧栏精简与完整推理显示（已完成）

- 将推理正文滚动区与固定操作栏拆成独立布局区域，正文不会再进入或被按钮区域遮挡。
- 默认窗口的推理正文高度不少于 560 px，`1120 × 760` 窗口仍不少于 300 px，并可滚动到解释末尾。
- 删除侧栏重复的“剩余/冲突”统计卡；剩余数继续显示在棋盘右上角六边形计数器中。
- 手动标记图例缩小为 `60 × 56`，删除按钮下方文字，保留悬停提示和无障碍名称。
- 求解器、生成器、私有答案隔离、行线索常显和截图入口状态均不变。

## 0.6.3：种子缓存与设置页（已完成）

- Easy/Hard 精确生成成功后自动缓存完整结构化结果；相同游戏 Build、后端版本、难度和种子再次请求时直接加载。
- 缓存采用带 schema、Build、后端和精确性门禁的 JSON，不使用 Pickle；损坏、过期和写入失败均自动回退到精确生成器。
- 缓存写入采用同目录临时文件与原子替换，私有答案仍只供揭示和测试，不进入求解器输入。
- 右下角工具栏增加设置入口；滚动设置页当前提供缓存状态、条目数、占用空间、存储路径和带确认的删除操作，结构可继续加入其他设置。
- 成品冒烟测试增加设置页、Easy/Hard 首次写入和第二次缓存命中验证；求解器与两套生成算法均未修改。

## 0.6.4：全屏长推理末尾显示修复（已完成）

- 修复全屏窗口中推理滚动条已经到底、最后一行仍被文本视口截断的问题。
- 移除 `QTextEdit` 视口与样式表中重复的底部内边距，改为在文本根帧增加 28 px 可滚动安全尾距。
- 长解释原文保持不变，复制文本不会多出用于占位的空行。
- 回归测试直接把光标移动到文档末尾，确认末行完整位于可见视口内并与固定操作栏保持至少 16 px 间距。
- 用 Hard seed 3 第 55 步在全屏布局滚动到底重新截图验证；求解器、生成器、缓存和设置页逻辑均未修改。

## 0.6.5：手动标记选中轮廓与原版式鼠标操作（已完成）

- 三个紧凑手动标记按钮保留原有填充和尺寸，未选中时继续使用白色细轮廓。
- 选中未知、蓝格、排除时，外轮廓分别改为黑色、橘色、蓝色，避免所有状态共用同一青色选中提示。
- 源码测试和冻结成品冒烟均依次点击三个按钮，检查互斥选中状态、颜色映射和实际渲染像素。
- 设置页可持久化启用原版式鼠标操作：左键排除、右键标记蓝色，再次同键点击恢复未知；关闭时保持现有手动工具流程。
- 开启原版式操作后暂时禁用手动状态按钮，关闭后恢复，避免两套点击语义同时出现。
- 求解器、生成器、缓存、推理显示、行线索和私有答案隔离逻辑均未修改。

## 0.6.x 后续：性能与大样本一致性

- 优化 Hard 复杂种子的 `Solver/Set` 与冗余线索裁剪性能。
- Easy 扩展到至少 30 个种子逐格、逐顺序、逐线索 100% 一致。
- 评估将 Easy Marvin 从无 Unity 托管核心继续移植为纯 Python 的收益与维护成本。

## 0.7.0：推理数据与棋盘动态联动（已完成）

- 将理由文本中的单个坐标、完整坐标数组和三方向行名称解析为独立、可测试的棋盘引用，不改变解释原文。
- 悬停引用时以虚线低幅呼吸轮廓预览全部相关格子；行引用同时强调对应外侧行线索。
- 点击引用后固定为实线高亮并加粗文字，再次点击取消；键盘 Enter/空格提供同等操作能力。
- 长坐标数组作为一个引用整体联动，避免每个坐标各自争抢悬停状态。
- 换步骤、应用、撤销、重置和换图时清除固定状态；不存在于当前棋盘的坐标安全忽略。
- 新增解析、Qt 交互和冻结成品冒烟回归；求解结论、解释原文、生成器、缓存和私有答案隔离均未修改。

## 0.7.1：提示框清理与高亮视觉优化（已完成）

- 删除推理引用字符格式中的提示文字，并拦截推理正文视口的 `ToolTip` 事件，悬停时不再出现遮挡内容的原生提示框。
- 棋盘引用覆盖层改为外层轻柔光与内层精细轮廓：悬停为细虚线，固定为实线，仍不改写格子填充、目标格或手动选中描边。
- 悬停只执行一次约 240 ms 的缓出淡入，不再持续呼吸；固定态完全静止，系统减少动态时直接显示最终静态轮廓。
- 保留点击固定/取消、Enter/空格键盘路径、文字加粗、行线索联动、长数组成组联动和全部状态清理语义。
- 新增无提示框、双层覆盖、虚实线状态、有限动画与减少动态回归；求解器、解释生成器、地图生成器和缓存核心均未修改。

## 0.7.2：全局唯一性分层门禁（已完成）

- 下一步调度严格按局部计数、排列、子集差分、全场剩余、全局唯一性的顺序逐层扫描全场。
- 任一较简单层找到必然步后立即返回稳定坐标，不再计算后续更复杂的局部层级，更不会启动 CP-SAT 全局反证。
- 公开的批量 `solve()` 接口保持原有候选收集语义，生成器、UI、缓存、解释模板和私有答案隔离不变。
- 新增三类门禁回归，并完整回放 Easy/Hard seed 1 到零未知格；每一步继续与原版私有答案一致，全局调用次数不增加。

## 0.7.3：启动方式与使用说明（已完成）

- 取消“应用当前建议”按钮的悬停提示框，保留直接应用功能和无障碍名称。
- 默认以有窗口最大化启动；设置页可持久化选择有窗口最大化、无边框全屏或普通窗口，并在下次启动时遵循。
- 启动时使用空盘面，不再展示模拟棋盘；改用四个手绘箭头说明种子生成、手动同步、下一步推理和设置入口。
- 使用说明可手动关闭，成功生成真实盘面后自动收起，也可从设置页重新打开。
- 求解器、解释生成、Easy/Hard 地图生成、种子缓存和私有答案隔离均未修改。

## 0.7.4：启动窗口下拉选择器（已完成）

- 启动窗口选择器默认折叠，关闭时只显示当前模式，不再常驻展开或形成深色大块。
- 折叠控件和弹出列表都使用白底、深色文字；当前项与悬停项用浅蓝底和青色文字表达。
- 保留明确下拉箭头、蓝色焦点轮廓、键盘展开与选择路径，三种启动模式和持久化语义不变。
- 源码、冻结成品和原生 Windows Qt 截图共同验证白底渲染、展开/收起与无深色表面回归。
- 求解器、解释生成、Easy/Hard 地图生成、种子缓存和私有答案隔离均未修改。

## 0.8.0：使用连续性与响应体验（已完成）

- 下一步推理移入后台线程，困难地图触发全局唯一性时不再阻塞 Qt 主界面。
- 盘面变化后丢弃过期推理结果；退出时请求安全取消，不强行终止求解线程。
- 新增重做与 `Ctrl+Z` / `Ctrl+Y` 快捷键，新操作会清空旧的重做分支。
- 真实局面自动原子保存；下次启动可选择恢复或放弃，损坏、篡改和不兼容存档会被安全拒绝。
- 设置页加入自动恢复开关、手动保存、载入和清除当前进度；手动存档使用 `.hexsave` JSON。
- Easy/Hard 生成器、推理规则、解释模板和私有答案隔离保持不变。

## 0.8.1：浅色确认框与全局推理易读化（已完成）

- 用应用内自绘的浅色确认框替换受 Windows 深色主题影响的系统确认框；恢复、清除进度和删除缓存使用同一套白底、高对比度视觉。
- 按钮改用“继续局面”“放弃并查看说明”等直接描述结果的中文文案，不再显示含义模糊的 `Yes / No`。
- 全局唯一性说明按“结论先看 → 为什么这样判断 → 关键条件概览 → 详细核查 → 术语说明”渐进展开，并用“试填后合法填法为 0”解释反证。
- 推理正文提高字号、行高和正文对比度，分区标题增加字重与间距；坐标、行名称及坐标数组的棋盘联动保持不变。
- 本版只调整确认交互、解释表达和排版，不修改地图生成、求解规则、全局唯一性门禁或推理结论。

## 0.8.2：全局推理性能（已完成）

- 每个全局步骤只构建一次 CP-SAT 模型；基础合法解为每个未知格提供一个见证值，只探测其相反值。
- 增加 25 ms 最大汉明距离合法解；只用具体合法解批量证明非固定格，超时自动回退逐格检查。
- 代表 Hard seed 1 完整回放中位数由 5.913 秒降到 0.549 秒，模型构建由 372 次降到 10 次，求解由 372 次降到 101 次；步骤序列和私有答案完全一致。
- 线程试验表明 2–12 线程均慢于单线程，因此不增加高资源模式；空闲预计算会因用户手动路径分叉、私有答案边界和低收益而不采用。
- 新增长线索局部规则会改变推理来源与步骤顺序，本版不把它混入等价性能补丁；待独立样本和解释评审后再决定。

## 0.9.0：模拟推演（已完成）

- 进入模拟推演时固定并保存当前真实盘面，之后的所有操作都发生在独立的模拟分支中。
- 模拟标记排除格或雷（蓝格）时，不揭示覆盖层下的提示或任何其他新信息；推演只使用进入模拟前已经公开的条件和用户的假设。
- 推演期间关闭“下一步推理”和应用建议功能，只提供手动模拟标记、独立撤销/重做、重置推演及退出推演。
- 每次模拟填块后可直接检测并高亮由公开线索证明的矛盾填块；多个假设共同造成矛盾时显示相关集合，不用私有答案判断实际错格。
- 模拟分支不得读取私有答案，也不得污染真实盘面、自动存档或正常撤销记录；退出时一键恢复到推演开始的局面。
- 使用独立撤销、重做和重置；设置、生成、存档、载入与清除进度在推演期间被阻止，避免跨模式状态泄漏。
- 公开约束冲突在后台线程检查，带修订号、取消和过期结果保护；求解超时不会被误报为矛盾。
- 源码 100 项回归、原生 Windows Qt 冲突画面和冻结成品 UI + Easy/Hard seed 1 冒烟均已通过。
- 完整需求和验收边界见 [`0.9.0 模拟推演需求`](docs/design/V090_SIMULATION_REQUIREMENTS.md)。

## 0.10.x：人类可理解的求解算法（顺延）

- 取消 `.hexcells` 导入导出计划，产品继续保持为独立种子求解器。
- 继续优化规则选择、推理顺序和解释表达，优先给出玩家能够在盘面上复核的短规则链，减少难以理解的大型全局约束说明。
- 自动测试继续保证每一步结论正确；同时预留较长的真人实玩验证期，由实际卡住的盘面检验解释是否真正有用。
- 真人测试中发现的难懂步骤应保存种子、难度、当前盘面和推理步骤，转成可重复验证的改进样本，不为赶版本压缩验证时间。

## 0.11.0：解释可视化（顺延）

- 将推理条件、相关格、排除过程和结论更直观地关联到棋盘。
- 以 `0.10.x` 的可读推理结果为基础；如果文字解释已经足够清楚，本版只做必要的轻量高亮和层次优化，不堆叠复杂动画。

## 0.12.x：全局 UI 与动画优化（顺延）

- 为整个版本系列预留全应用的界面、布局、视觉一致性和动画体验优化，不与单个功能补丁混做。
- 统一棋盘、侧栏、设置、提示与状态反馈，继续保持游戏风格，并兼顾全屏、窗口模式和“减少动态”需求。
- 在主要功能稳定后再开始，避免算法和模拟推演仍在变化时重复返工。

## 1.0.0：稳定发布（暂不排期）

- 不急于上线；在模拟推演、算法真人验证、解释可视化及 UI 优化稳定后再确定发布时间。
- 正式发布前仍需通过种子回归、完整求解回放、私有答案隔离和打包运行验证。
